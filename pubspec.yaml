name: oasis_coin
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.1
  any_base: ^2.0.0

  cupertino_icons: ^1.0.2
  uuid: ^3.0.4
  eosdart_ecc: ^0.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true